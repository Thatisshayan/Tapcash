import PageMetadata from "@/components/PageMetadata";

export default function Head() {
  return (
    <PageMetadata
      title="Dashboard | TapCash"
      description="Track your TapCash balance, offers, missions, and earning progress in one place."
      canonical="/dashboard"
      noindex
    />
  );
}
