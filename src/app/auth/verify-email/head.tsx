import PageMetadata from "@/components/PageMetadata";

export default function Head() {
  return (
    <PageMetadata
      title="Verify Your Email | TapCash"
      description="Verify your TapCash account email before unlocking offers, cashout, and referral earnings."
      canonical="/auth/verify-email"
      noindex
    />
  );
}
