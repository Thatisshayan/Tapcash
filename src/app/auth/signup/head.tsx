import PageMetadata from "@/components/PageMetadata";

export default function Head() {
  return (
    <PageMetadata
      title="Create Account | TapCash"
      description="Join TapCash to start earning coins from verified offers, referrals, and reward missions."
      canonical="/auth/signup"
      noindex
    />
  );
}
