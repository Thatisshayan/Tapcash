import PageMetadata from "@/components/PageMetadata";

export default function Head() {
  return (
    <PageMetadata
      title="TapCash Privacy Policy"
      description="Read how TapCash collects, protects, and uses account, fraud, and transaction data across the rewards platform."
      canonical="/privacy"
    />
  );
}
