import PageMetadata from "@/components/PageMetadata";

export default function Head() {
  return (
    <PageMetadata
      title="Referrals | TapCash"
      description="Invite friends to TapCash, track referral earnings, and grow passive rewards through your link."
      canonical="/referrals"
      noindex
    />
  );
}
