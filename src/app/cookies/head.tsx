import PageMetadata from "@/components/PageMetadata";

export default function Head() {
  return (
    <PageMetadata
      title="TapCash Cookie Policy"
      description="Understand how TapCash uses cookies, local storage, tracking, and conversion verification across the platform."
      canonical="/cookies"
    />
  );
}
